你是 Reasonix，一个专注代码实现的编程助手。请根据以下项目上下文帮我排查问题。

## 项目：study-assistant（知识点学习助手）

### 定位
自学辅助工具：用户创建学习计划 → 添加知识点列表 → AI 逐个生成详细讲解（Markdown）→ 用户可追问，历史影响后续讲解。

### 技术栈
- 前端：React 19 + Vite 8（client/src/）
- 后端：Node.js + Express 4（server/），ESM（"type": "module"）
- AI：OpenAI 兼容 API（client 存 localStorage，server 读 .env 或 req.body）
- 存储：JSON 文件（server/data/learn/plans/{id}.json + .bak），原子写入 tmp+rename
- 端口：3001（后端），5173（Vite 开发服务器，proxy /api → 3001）

### 重要文件
- client/src/App.jsx — 主组件，3 个视图：PlanList → PlanView → TopicDetail
- client/src/components/PlanList.jsx — 计划列表，含 AI 导入（粘贴文本 / 选择文件）
- client/src/components/PlanView.jsx — 知识点列表+添加，含文件导入（GBK/UTF-8 自动检测）
- client/src/components/TopicDetail.jsx — 知识点详情+追问，自动触发生成+轮询进度；含 Mermaid 图表渲染集成；支持 .md 和 .html 双格式导出；底部有「学完了，返回列表」按钮
- client/src/components/MermaidDiagram.jsx — 【新增】Mermaid 图表渲染组件，使用 mermaid.render() 安全转换为 SVG
- client/src/api.js — API 封装，importPlan 会带 localStorage 中的 apiKey
- server/engine/learn-store.js — 数据层（CRUD + 原子写入 + 备份 + 通知 flag）
- server/engine/learn-engine.js — AI 生成引擎（流式 OpenAI API）
- server/engine/learn-prompts.js — Prompt 模板。**注意**：讲解和追问的 system prompt 中，关于图片的规则已从「禁止任何图片」改为「鼓励使用 Mermaid 语法（```mermaid）绘制图表，同时保留禁止虚假 img 标签」
- server/routes/learn.js — 所有 API 路由

### API Key
支持三种方式：req.headers.x-api-key / req.body.apiKey / process.env.OPENAI_API_KEY
前端在 api.js 中调用 importPlan/generateDetail/askQuestion 时会自动从 localStorage 取 apiKey 附加到 body。

### 已知风险
- 文件导入的 GBK/UTF-8 编码检测用的是 CJK 字符计数法（utf8 vs gbk 解码后比较中文数）
- 无 API Key 校验（只在调用 AI 时报错）
- 后端数据目录不含 .gitignore
- PlanList 和 PlanView 都有文件导入功能（PlanList 是 AI 导入，PlanView 是纯文本导入）
